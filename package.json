{
  "dependencies": {
    "bindings": "1.5.0"
  },
  "devDependencies": {
    "node-gyp": "5.0.3"
  },
  "main": "src",
  "scripts": {
    "build": "node-gyp clean configure build",
    "prettify": "clang-format -i **/*.cc"
  }
}
